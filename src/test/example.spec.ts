import "@testing-library/jest-dom/vitest";

import { describe, it, expect } from "vitest";
import BandaCadastro from "@/pages/BandaCadastro";


describe('should first test', () => {
  it('first test', () => {
    const a = 1
    expect(a).toEqual(1)
  })
});
